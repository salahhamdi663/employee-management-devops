apiVersion: v1
kind: PersistentVolume

metadata:
  name: uploads-pv

spec:
  capacity:
    storage: 1Gi

  accessModes:
    - ReadWriteOnce

  persistentVolumeReclaimPolicy: Retain

  hostPath:
    path: /data/uploads
